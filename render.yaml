# Render Blueprint — provisions backend, database, and frontend together.

databases:
  - name: leadcrm-db
    databaseName: leadcrm
    plan: free

services:
  # --- Backend API (FastAPI) — web service accepts plan: free ---
  - type: web
    name: leadcrm-api
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: leadcrm-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        sync: false
      - key: PYTHON_VERSION
        value: "3.12.7"

  # --- Frontend (React static site) — always free; no plan field ---
  - type: web
    name: leadcrm-web
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
