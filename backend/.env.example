# Copy to .env and adjust. A random SECRET_KEY is generated if unset,
# but set a fixed one in production (openssl rand -hex 32).
SECRET_KEY=change-me-to-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
DATABASE_URL=sqlite:///./leadcrm.db
